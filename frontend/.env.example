# Frontend environment variables
# VITE_API_URL — backend URL (puste = ten sam origin, np. przez proxy vite)
VITE_API_URL=http://localhost:3001
# VITE_API_KEY — klucz API backendu (opcjonalne, jeśli API_KEY ustawione na backendzie)
VITE_API_KEY=
